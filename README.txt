To run program, navigate to BB-coding-challenge directory and use command:
$ ruby run_terminal.rb
This will run the program with the products and prices given in the instructions.

To run the program with different products/prices, use the command:
$ ruby run_terminal.rb [file]
where file is the path to a json file (see included 'prices.json' for formatting).

The test cases give in the instuctions are located within 'terminal_test.rb'. To run these tests, use command:
$ ruby terminal_test.rb